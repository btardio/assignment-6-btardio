# ### workspace layer auto-generated by devtool ###
MACHINE = "qemuarm64"
BB_NUMBER_THREADS = "71"
PARALLEL_MAKE = "-j 71"

BBPATH =. "${LAYERDIR}:"
BBFILES += "${LAYERDIR}/recipes/*/*.bb \
            ${LAYERDIR}/appends/*.bbappend"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb"

#BBFILE_COLLECTIONS += "ruby-"

BBFILE_COLLECTIONS += "workspacelayer"
BBFILE_PATTERN_workspacelayer = "^${LAYERDIR}/"
BBFILE_PATTERN_IGNORE_EMPTY_workspacelayer = "1"
BBFILE_PRIORITY_workspacelayer = "99"
LAYERSERIES_COMPAT_workspacelayer = "${LAYERSERIES_COMPAT_core}"
# LAYERDEPENDS_workspacelayer = "ruby"
#
#
#
#
#
#
#
#
#
#
#
#



# We have a conf and classes directory, add to BBPATH
#BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
#BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
#            ${LAYERDIR}/recipes-*/*/*.bbappend"

#BBFILES += "${LAYERDIR}/recipes-devtools/ruby/*.bb"

#BBFILE_COLLECTIONS += "meta-aesd"
#BBFILE_PATTERN_meta-aesd = "^${LAYERDIR}/"
#BBFILE_PRIORITY_meta-aesd = "6"

#LAYERDEPENDS_meta-aesd = "core"
#
#AYERSERIES_COMPAT_meta-aesd = "hardknott kirkstone"

